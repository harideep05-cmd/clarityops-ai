# Setup

The complete, maintained instructions are in the [root README](../README.md), including
Windows PowerShell commands that do not require activation-script permissions.

The early setup instructions were replaced after the original prototype became the
knowledge MVP. Use the root README's pinned dependencies, private environment helper,
explicit embedding download, backend startup and frontend proxy instructions.
